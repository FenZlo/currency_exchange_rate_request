[project]
name = "currency_exchange_rate_request"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "FenZlo", email = ""},
]
dependencies = [
    "httpx>=0.27.0",
    "loguru>=0.7.2",
    "sqlmodel>=0.0.16",
]
requires-python = "==3.11.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
